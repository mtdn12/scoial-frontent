import { makeStyles } from '@material-ui/styles'

const useStyles = makeStyles({
  container: {
    maxWidth: 800,
    margin: '0 auto',
    paddingTop: 16,
  },
  textField: {
    margin: '8px 0',
  },
  loadingItem: {
    position: 'absolute',
    right: 135,
  },
})

export default useStyles
